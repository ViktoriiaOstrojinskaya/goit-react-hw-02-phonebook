export const ContactList = () => (
  <ul>
    {/* {this.state.contacts.map(contact => (
      <li key={contact.id}>
        <p>{contact.name}</p>
        <p>{contact.number}</p>
      </li>
    ))} */}
  </ul>
);
